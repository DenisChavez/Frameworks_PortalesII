document.addEventListener('DOMContentLoaded', function () {
    //variables para que el sideNavBar funcione
    {
        let elems = document.querySelectorAll('.sidenav');
        let instances = M.Sidenav.init(elems);
    }
});
